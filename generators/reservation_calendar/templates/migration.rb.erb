class Create<%= class_name.classify.pluralize %>And<%= subclass_name.classify.pluralize %> < ActiveRecord::Migration
  def self.up
    create_table :<%= class_name.pluralize %> do |t|
      t.string :name
      t.timestamps
    end
    create_table :<%= subclass_name.pluralize %> do |t|
      t.column :<%= class_name.pluralize %>_id, :integer
      t.column :date, :date
      t.timestamps
    end
  end

  def self.down
    drop_table :<%= subclass_name.pluralize %>
    drop_table :<%= class_name.pluralize %>
  end
end
