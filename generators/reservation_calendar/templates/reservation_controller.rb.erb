class <%= class_name.classify.pluralize %>Controller < ApplicationController
  
	# GET /<%= class_name.pluralize %>/1
  def show
    @<%= class_name %> = <%= class_name.classify %>.find(params[:id])

    respond_to do |format|
      format.html # show.html.erb
    end
  end
  
end